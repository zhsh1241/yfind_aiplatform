spring:
  datasource:
    url: ${POSTGRES_TEST_JDBC_URL:${POSTGRES_JDBC_URL:jdbc:postgresql://localhost:5432/yfind_aiplatform_test}}
    driver-class-name: org.postgresql.Driver
    username: ${POSTGRES_USER:yfind}
    password: ${POSTGRES_PASSWORD:yfind_local_password}
