# Deep Interview Archive

本次任务来自用户明确要求：先理解项目，重点分析 `docs/business/` 与 `docs/prototype/`，然后完成项目脚手架（包括 AI 脚手架）必要改造。

## 澄清结论

- 当前目标不是实现业务功能，而是建立重建基线。
- 已清空的 backend/frontend/deploy 不应作为事实来源。
- `docs/business/` 与 `docs/prototype/` 是后续规划与实现的权威输入。
- AI scaffold 需要适配当前清空状态，避免 gate 误跑旧目录。
