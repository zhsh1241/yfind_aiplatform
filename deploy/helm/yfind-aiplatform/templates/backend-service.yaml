apiVersion: v1
kind: Service
metadata:
  name: {{ include "yfind-aiplatform.fullname" . }}-backend
spec:
  selector:
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.port }}
