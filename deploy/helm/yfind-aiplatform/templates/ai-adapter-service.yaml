apiVersion: v1
kind: Service
metadata:
  name: {{ include "yfind-aiplatform.fullname" . }}-ai-adapter
spec:
  selector:
    app.kubernetes.io/component: ai-adapter
  ports:
    - name: http
      port: {{ .Values.aiAdapter.service.port }}
      targetPort: {{ .Values.aiAdapter.service.port }}
